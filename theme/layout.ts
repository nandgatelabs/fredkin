/** Shared layout tokens for dialogs and readable content width. */
export const layout = {
  dialogMaxWidth: 440,
  /** Centered app column on web / desktop. */
  contentMaxWidth: 880,
  /** Header search pill cap on web. */
  searchMaxWidth: 420,
} as const;
